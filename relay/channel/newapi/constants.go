package newapi

// ChannelName is the upstream New API relay channel identifier.
const ChannelName = "newapi"

// ModelList is empty because New API channels fetch models dynamically from /v1/models.
var ModelList = []string{}
