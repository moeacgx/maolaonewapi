# 渠道供应商与上游重试 `.303` 发布记录

## 目标与范围

- 发布版本：`v1.0.0-rc.10.1.10.303`。
- 变更基线：`v1.0.0-rc.10.1.10.302...v1.0.0-rc.10.1.10.303`。
- 渠道管理：恢复 `vendor_id` 保存、详情回填、显式清除、供应商计数与筛选，并恢复
  Default 渠道编辑和列表展示。
- 中继重试：恢复嵌入错误文本中的可信上游状态码识别，并在失败时驱逐失效的渠道亲和缓存。
- 部署目标：仅 CloudSSH 项目“API中转站”的 `serverId=52`，Compose 目录
  `/home/docker/zzapi`。
- 应用服务：`zzapi-slave-1`、`zzapi-slave-2`、`zzapi`，按此顺序逐个滚动更新。
- 明确不更新：`zzapi-postgres`、`zzapi-redis` 和受保护的 `maolaoapi`。

## 兼容性与安全边界

- 渠道迁移只新增可空的 `vendor_id` 及普通索引，SQLite、MySQL 和 PostgreSQL 均由 GORM
  迁移；旧版本可忽略该列，因此允许 `.302` 与 `.303` 在滚动窗口内短时并存。
- 更新接口中省略 `vendor_id` 表示保留绑定，数字表示设置绑定，显式 `null` 表示清除绑定。
- 供应商字段属于普通渠道元数据，不因敏感渠道写权限缺失而锁定；密钥等敏感字段权限保持不变。
- 回滚应用版本时新增数据库列可以保留，不执行删列或数据库恢复。

## 发布门禁

1. PR `#159` 的后端、前端与质量检查全部通过后合并到 `custom-main`。
2. 等待 PR 合并触发的 `closed` 事件 CI 在远程主分支再次通过。
3. 推送发布分支和 `v1.0.0-rc.10.1.10.303` 标签，等待 Linux Release 与 GHCR
   多架构镜像工作流成功。
4. 核对 Release 二进制、校验文件、`linux/amd64`、`linux/arm64` 和版本镜像 manifest。

## 远端更新与验收

1. 更新前读取 Compose 项目、应用镜像、容器状态、重启次数、本地端口和公网状态。
2. 记录 `.302` 镜像不可变摘要，并创建带版本号的 Compose 备份。
3. 只把三个应用服务的镜像标签更新为 `.303`，逐个执行无依赖强制重建；每个节点验证
   `running/healthy`、重启次数 `0`、容器内版本和本地 `/api/status` 后再继续。
4. 全部节点完成后核对 `18097/18098/18099`、公网 `/api/status`、Nginx 分流和启动日志。
5. 使用 Root 登录态验证新建或编辑渠道可保存供应商，重新打开仍能回填，列表供应商列与顶部
   供应商筛选均正确；再执行一次低成本鉴权请求验证基础中继链路。

## 回滚

任一节点更新失败时立即停止后续节点，使用发布前记录的 `.302` 镜像摘要和 Compose 备份，
仅回滚已修改的应用服务。不得重建 PostgreSQL、Redis，不得操作 `maolaoapi`；回滚后重新
核对三个应用端口、容器健康、重启次数和公网状态。

## 实际发布结果

- PR `#159` 已合并到 `custom-main`，合并提交为 `cc8fd2bbe`；PR CI `33748912699` 和
  合并后的 `closed` 事件 CI `33749266177` 均成功。
- 发布标签 `v1.0.0-rc.10.1.10.303` 指向提交 `2ccb2e8a8`。Linux Release
  `33749896701` 与 GHCR 多架构工作流 `33749896712` 均成功，Release 地址为
  `https://github.com/moeacgx/maolaonewapi/releases/tag/v1.0.0-rc.10.1.10.303`。
- GHCR 多架构 manifest 为
  `sha256:6844aa78a47bccd0e3e7d960384202e990136697eb9c565db17fa3de00ce5abc`，
  生产主机拉取后的 amd64 镜像 ID 为
  `sha256:46fe79b71bd5d99fe1e93c041e83a2039c337720cbf1d2196e3b2b1a2623e940`。
- CloudSSH 预检作业为 `467b3994-36af-4155-b4a9-1d65c9a97ab1`。Compose 已备份为
  `docker-compose.yml.bak-v1.0.0-rc.10.1.10.302-to-v1.0.0-rc.10.1.10.303`，备份
  SHA-256 为 `518e99d4efe207370bfbafe630a7de3baf56b0c8dc6bedfa73a8f6fbbba38d90`；
  更新后 Compose SHA-256 为
  `ad85d82b2bd1325d3ce202c86da1e27ae5b4dee37d3b1bbed9b3cda403e0c00c`。
- Compose 准备作业 `f1e6ee3a-6aff-462c-8ee6-a3cf6214a994` 已完成镜像拉取、备份和配置
  替换，但最后一条只读 Docker 模板输出因类型不兼容返回失败；后续只读作业
  `c6128bd8-6de3-43e0-9e12-b534116a35c8` 确认配置、备份和镜像均正确后才开始滚动更新。
- 滚动更新作业依次为：`zzapi-slave-1`
  `f8c8c9f4-afa6-4368-824b-064c878e2cd6`、`zzapi-slave-2`
  `3473bbe8-43c5-436c-8685-d06b469c6a29`、`zzapi`
  `d2226e8e-cdc3-4ab8-ac72-bbd568c21eec`。三个应用均为 `running/healthy`、重启次数
  `0`，本地端口 `18097/18098/18099` 与公网状态均返回 `.303`，启动日志无关键错误。
- PostgreSQL 和 Redis 未重建，镜像 ID 与更新前一致且重启次数为 `0`。PostgreSQL 已确认
  `vendor_id` 列和索引各存在一份，已有 10 条非空供应商绑定仍保留。
- 最终核心核验由 `11516184-897f-4339-908a-01da9abe113f` 完成：连续 18 次公网状态请求
  均返回 `.303`，首页返回 `200`，未登录渠道接口返回预期的 `401`。
- 日志型 `lbprobe` 不适用于当前状态接口，因为应用容器不记录该请求。只读现场核验
  `719df654-ff3f-4210-94ed-c925288882ba` 进一步确认 Nginx 当前只把公网域名转发到
  `127.0.0.1:18097`，未引用 `18098/18099`；因此两个 secondary 虽已同步更新和直连验收，
  当前不承接公网流量。本次未越权修改该既有入口配置。

## 尚待登录态验收

当前执行环境未使用生产 Root 凭据，也未修改任何现有渠道，因此未直接执行供应商保存、重新打开
回填和顶部筛选的生产交互验收。代码路径已经由本地与 CI 回归测试覆盖，生产数据库迁移和历史
供应商绑定已确认；后续可由持有 Root 登录态的管理员完成页面交互复核。
