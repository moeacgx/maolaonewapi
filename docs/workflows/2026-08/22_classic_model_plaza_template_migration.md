# Classic 模型广场新版模板布局迁移

日期：2026-08-22

## 目标

将 `web/src/features/pricing/` 作为唯一视觉和交互基准，完整移植其模型广场
页面层级、筛选分区、工具栏、卡片、表格和详情抽屉到 Classic；Classic 仅保留
自身真实数据源和已有的计费、性能、折扣与批量操作能力。

## 范围

- 页面采用新版的居中标题、模型计数、搜索框、筛选栏和结果区布局，文案、
  折叠顺序、筛选 Chip 和工具栏主层级均以新版实现为准。
- 宽度不小于 `1280px` 时使用粘性筛选侧栏；较窄宽度使用同一筛选内容的右侧抽屉。
- 筛选分组、供应商、标签、计费类型和端点类型均可独立折叠；筛选项继续显示
  数量、供应商图标和分组倍率。
- 卡片、表格和详情抽屉使用新版的图标、价格、元信息、操作和性能摘要层级；
  不额外设计供应商顶边、语义色分区或彩色卡片。
- 加载骨架与实际卡片共用同一响应式网格和信息密度，避免加载完成时布局跳变。
- 工具栏保留 Classic 的充值价格、货币、倍率、Token 单位、批量复制、
  视图切换和计费说明入口；新增能力收纳到不改变新版主工具栏层级的更多操作中，
  移动端也必须提供等价入口。
- 详情抽屉采用新版的紧凑标题、分区和三标签布局；概览、性能和端点内容
  继续使用 Classic 的动态计费、默认规格价、路由规格价、分组倍率和真实性能。
- API 标签以新版的代码示例和鉴权区块为主结构；真实端点只作为示例切换来源，
  不再额外渲染旧版独立端点表，也不引入新版 mock 参数或限流数据。
- 页面严格复用新版的中性配色、边框、阴影、背景光晕和响应式尺寸，不添加
  Classic 专有的颜色主题、卡片装饰或替代布局。

## 数据与行为约束

- 数据仍由 `useModelPricingData` 提供，模型、供应商、分组、价格和性能接口均未改变。
- 筛选计数继续通过 `usePricingFilterCounts` 计算；分组筛选继续调用
  `handleGroupClick`，使列表筛选分组和价格展示分组始终一致。
- 重置筛选同时恢复 `filterGroup` 与 `selectedGroup`，避免列表已重置而
  卡片、表格或计费说明仍使用旧分组倍率。
- 表格列工厂不再在 memo 回调中读取视口 Hook；卡片/表格切换后仍保持稳定的
  Hook 顺序，移动端固定列规则继续由页面当前视口决定。
- 卡片继续使用 Classic 的价格计算、动态计费摘要、折扣标签和
  `performanceMap`，不复制新版模板的 mock 性能、限流、参数或应用排行数据。
- 详情抽屉在固定价模型上同时展示 `model_price_variants` 和
  `model_route_price_variants`，规格规则仍是最终价格，不能与基础价相加。
- `siteDisplayType === 'TOKENS'` 时仍不展示充值价格与货币选择。

## 可访问性与响应式

- 筛选 Chip 使用原生按钮和 `aria-pressed`；分区折叠和“展开更多”使用
  `aria-expanded`，图标操作保留可访问名称和悬浮提示。
- 卡片、筛选项和工具栏在 `320px` 起允许换行或截断，避免横向溢出。
- 移动端将 Classic 专有的充值价格、货币、倍率、Token 单位、批量复制和计费说明
  收纳到“更多操作”；计费说明仅在用户主动打开后显示，不遮挡新版首屏。

## 验证

- 通过浏览器对照新版与 Classic 的 `390px` 和 `1280px` 视口；标题、搜索、
  侧栏、筛选抽屉、工具栏、卡片网格、表格和详情抽屉均按新版结构核验，未出现
  横向溢出或操作重叠。
- 实测供应商筛选、移动端重置、充值价格与 USD/CNY/自定义币种、倍率、Token
  单位、卡片/表格切换、详情概览/性能/API 标签；移动端更多操作可访问所有
  被收纳的 Classic 二开控制。
- 执行 `pnpm exec prettier --check`、目标 JSX 的 `pnpm exec eslint`、
  `pnpm run build` 及 `git diff --check`。

## 兼容性

本次无后端接口、模型 ID、价格计算规则、额度、鉴权或跨项目契约变更。

## 2026-08-23 模型广场白屏回归修复

### 问题与根因

Classic 模板的 `/pricing` 顶栏渲染带图标的自定义导航项时会白屏。顶栏将
`getCustomNavIcon` 的 JSX 返回值当作组件再次以 `<Icon />` 渲染，触发 React
运行时的无效元素类型错误。该问题仅影响页面展示，不涉及模型、价格、性能或
折扣数据链路。

### 修复范围

- `PricingTemplateHeader` 改用 `getCustomNavIconComponent` 获取 Lucide 组件类型，
  保持现有 `<Icon />` 渲染方式。
- 保持 `Navigation` 对 `getCustomNavIcon` 的直接 JSX 渲染不变，避免改变其他
  Classic 导航的颜色和选中态行为。
- 增加顶栏自定义图标回归用例：验证图标查找结果可作为 React 组件渲染，并要求
  模型广场顶栏使用组件查找 API。

### 验证

- 在修复前，新增用例稳定复现顶栏错误；修复后执行
  `bun test src/components/layout/headerbar/__tests__/pricing-template-header.test.jsx` 通过。
- 对修改的 JSX 和测试执行 Classic ESLint、Prettier 校验和 `git diff --check`，均通过。
- Classic 生产构建通过；发布后需在实际自定义导航配置下访问 `/pricing`，确认
  页面、筛选、卡片和详情均正常渲染且控制台无 React 运行时错误。

## 2026-08-23 模板细节对齐

### 修复范围

- 顶栏继续以新版 `PublicHeader` 的固定定位、滚动收缩和移动端层级为准。
- 价格标签展示所有非原价的综合价格调整：低于原价显示折扣，高于原价显示倍率；
  恰为原价的容差区间不展示标签。卡片分组名称始终使用实际参与价格计算的分组。
- 固定价模型的规格规则仅在“规格价格”区展示；“按分组定价”使用规格价格范围，
  不把每条规格规则重复展开为表格列。分组表首列保持不换行，并允许横向滚动。

### 验收口径

- 重点检查固定顶栏、加价倍率与折扣标签、长分组名称，以及包含多条规格规则的
  固定价模型；同时确认动态计费、性能、折扣说明和批量操作仍可访问。

### 验证结果

- `bun test src/components/layout/headerbar/__tests__/pricing-template-header.test.jsx`
  通过；`billing/utils.test.mjs` 的价格调整边界断言通过。
- 修改文件的 Prettier、JSX ESLint 与 `git diff --check` 均通过。
- 使用 `bun.lock` 还原依赖后，Classic `npm run build` 通过；发布后仍需以真实
  分组和价格数据在 zzapi 的 `/pricing` 页面完成浏览器验收。

## 2026-08-23 顶栏滚动容器回归修复

### 问题与根因

Classic 的页面布局存在多个 `section.semi-layout`。原顶栏仅用
`querySelector` 监听第一个外层容器，而实际滚动发生在内层容器；因此页面滚动后
顶栏未进入收缩状态，透明导航会与模型卡片内容重叠。

### 修复范围

- 顶栏收集并监听全部 Classic 布局滚动容器，只要任一容器滚动超过 20px 即进入
  新版模板的悬浮收缩样式。
- 保留被动滚动监听，并且仅在收缩状态实际改变时更新 React state，避免滚动过程
  中的无效重渲染。
- 增加多滚动容器回归用例，覆盖内层容器滚动、窗口滚动及 20px 边界。

### 验收口径

- 在真实 `/pricing` 页面向下滚动后，顶栏应收缩为带背景和阴影的悬浮导航，不得
  遮挡卡片或筛选内容。
- 折扣、加价倍率、实际价格分组、动态计费、性能、计费说明和批量操作保持不变。

### 验证

- `bun test src/components/layout/headerbar/__tests__/pricing-template-header.test.jsx` 通过。
- 修改文件的 Prettier、ESLint 与 `git diff --check` 通过。
- Classic `npm run build` 通过；发布后在 zzapi 的真实滚动容器中复验顶栏收缩。

## 2026-08-23 汇率折扣与长导航回归修复

### 折扣口径

- 模型卡片和详情中的综合折扣统一为
  `充值汇率 ÷ 美元实际汇率 × 实际参与计价的分组倍率`。例如充值汇率
  `1.03`、美元实际汇率 `6.72`、分组倍率 `0.15` 时，综合因子为
  `0.023`，展示为 `0.2 折`。
- 汇率只读取状态接口的 `price` 与 `usd_exchange_rate`，不在前端硬编码
  市场汇率。测试环境若配置了不同的 `usd_exchange_rate`，折扣展示会随该
  配置变化；该展示不改变后端实际扣费规则。

### 布局修复

- 收缩态顶栏保持新版悬浮样式，但桌面端使用完整容器宽度；自定义导航和用户名
  区分别保留在可用宽度两端，不再互相重叠。
- 用户名区额外向右保留 `4px` 的可见间距，使自定义导航留在左侧且不会贴住登录
  用户名；未登录时相同间距也适用于登录按钮。
- 因 Classic 可配置额外导航项，窄于 `1100px` 时提前收纳为移动菜单，避免
  平板宽度出现导航与用户区碰撞。
- 卡片页脚恢复新版两行网格：实际计价分组与计费类型在第一行，元数据在第二行，
  性能摘要跨两行，长分组不会被元数据挤成零宽。
- 表格分组、标签和端点最多横向显示两项，其余以 `+N` 收纳并在悬浮提示中展示；
  单元格不再换行形成竖排分组名。

### 兼容性与验证

- 本次只调整折扣展示口径与 Classic 模型广场布局，不修改模型、分组、汇率配置、
  后端计价或额度数据。
- 回归检查覆盖汇率因子、顶栏收缩、卡片分组与表格溢出；发布后需在真实长导航和
  登录用户名状态下，以桌面和窄屏视口完成浏览器验收。
- 计费工具回归断言覆盖 `1.03 ÷ 6.72 × 0.15 = 0.023`，并确认前端标签输出
  `0.2 折`；测试环境的实际显示仍以其状态接口返回的汇率为准。

## 2026-08-23 顶栏用户名空间修复

### 问题与根因

此前只给用户名容器增加了 4px 微调，但自定义导航、语言、主题、公告和用户名仍处于
同一个右对齐 Flex 组。宽屏下这一改动几乎不可见，长导航也会持续向用户名区挤压。

### 修复范围

- 桌面容器上限扩展为 1440px，让宽屏时导航左移、用户名右移，保留新版的顶栏外观与
  滚动收缩效果；原有 Flex 顺序保持不变。
- 原有用户名区独立间距继续保留，保证长用户名和可配置导航项不会重叠。

### 验收口径

- 在 1280px 与宽屏登录态下，导航文字与右侧用户名区之间必须留有可见空白，且宽屏时
  用户名区应随容器右缘明显向右移动。
- 模型广场的筛选、卡片、表格、折扣、性能、动态计费和批量操作均保持原有行为。

## 2026-08-23 顶栏用户名菜单宽度回归修复

### 问题与根因

模型广场顶栏为语言、主题和通知图标设置了统一的 `32px` 按钮宽度；该选择器
同时命中了登录态的用户名下拉按钮，导致用户名和下拉箭头被压缩或溢出。

### 修复范围

- 仅在用户名容器内覆盖按钮宽度，恢复 `auto` / `max-content` 的自然宽度并保持
  左对齐。
- 保留图标按钮的固定尺寸、导航间距、折扣、性能和模型分组展示逻辑。

### 验证

- 静态回归断言覆盖用户名按钮宽度覆盖规则。
- 公网桌面视口验证顶栏容器宽度为 `1440px`，登录按钮与导航均无溢出；移动视口
  验证页面宽度无横向滚动。
