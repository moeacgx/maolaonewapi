## 变更描述 / Description

Classic 模板在手机端原先只提供导航链接和登录/控制台入口，个人中心、通知、语言和主题操作仍停留在桌面顶栏；手机端顶栏本身已经收纳在右侧菜单中，用户无法在移动菜单内完成这些常用操作。

本 PR 将 Classic 手机菜单扩展为完整的账户工具区：复用现有 `UserArea`、`NotificationButton`、`LanguageSelector` 和 `ThemeToggle` 组件，并保留底部的登录/控制台入口。手机顶栏移除重复的主题按钮，仅保留菜单切换按钮；菜单主体可滚动，避免小屏内容被截断。

同时修复 Classic 退出登录请求仍调用旧 `GET /api/user/logout` 路径的问题。当前后端契约要求 `POST /api/user/auth/logout`，旧路径会返回 403，导致用户看到 “Request failed with status code 403” 且前端状态无法正常清理。

## 变更类型 / Type of change

- [x] 🐛 Bug 修复 (Bug fix)
- [x] ✨ 新功能 (New feature)
- [x] 📝 文档更新 (Documentation)

## 关联任务 / Related Issue

- 无

## 实现要点

- 手机侧栏新增个人中心入口，复用桌面端账户菜单和退出逻辑。
- 手机侧栏新增通知未读数按钮、语言选择和主题切换。
- 手机顶栏保留菜单按钮，避免同一操作重复出现。
- 为新增控件补齐所有 Classic 语言的可访问文案。
- 增加退出接口、移动菜单结构和多语言契约回归测试。
- 增加开发工作流记录，并更新开发文档索引。

## ✅ 提交前检查项 / Checklist

- [x] 人工确认：已整理并撰写本描述。
- [x] 非重复提交：已检查开放 PR 和 Issue，未发现同类待处理项。
- [x] 变更理解：已确认退出接口与移动菜单组件的现有契约。
- [x] 范围聚焦：仅修改 Classic 模板、相关语言文案、测试和开发文档。
- [x] 本地验证：定向 Node.js 契约测试通过（4/4）；`git diff --check` 通过。
- [x] 安全合规：未引入凭据或放宽会话安全校验。

## 📸 运行证明 / Proof of Work

```text
node --test web/classic/src/hooks/common/__tests__/logout-contract.test.mjs web/classic/src/components/layout/headerbar/__tests__/mobile-menu-contract.test.mjs web/classic/src/components/layout/headerbar/__tests__/mobile-controls-i18n.test.mjs
tests 4
pass 4
fail 0
```

Classic 生产构建未执行：当前工作区未安装 Classic 前端依赖（缺少 `vite`）。Default 模板未修改。
